# アーカイブ画像の二重送信防止アップデート

## 変更内容

- 記事URL・DiscordチャンネルID・画像URLの組み合わせで送信履歴をSQLiteへ保存します。
- 途中で一部の画像だけ送信できた場合でも、成功済み画像は次回の巡回で再送しません。
- 送信直前に `sending` として予約し、送信成功後に `sent` として確定します。
- RailwayがDiscord送信直後に再起動した場合、`sending` の画像も自動再送しません。
- Discord APIが明確に送信失敗を返した場合だけ予約を解除し、次回再試行します。
- 低通信量モードと添付送信モードの両方に対応しています。

## DB

既存の `/data/archive.db` に次のテーブルが自動作成されます。

- `archive_image_deliveries`

既存データベースを削除する必要はありません。

## 注意

分散処理では「Discordでは送信成功したが、成功応答を受け取る直前に通信断」という状態を完全には判定できません。
この版は二重送信防止を優先し、送信直前の `sending` 状態も再送対象から除外します。
